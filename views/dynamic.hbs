{{> header this.menu }}
{{> video-bg }}


<main>
  <div class="container">
    <div class="row">
      <div class="col-12 fh flex in-column center-center light">
      {{#each main}}
        {{> (lookup this 'name') (lookup this 'context')}}
      {{/each}}
      </div>
    </div>
  </div>
</main>